// Basic Exercises involving Pairs

// Swaps the elements of a pair
swap (a,b) = (b,a)

// Returns the first element of a pair
first (a,b) = a

// Returns the second element of a pair
second (a,b) = b

// Applies a function to both elements of a Pair
mapa fn (a,b) = (fn(a), fn(b)) 
